{
  "name": "@ai-defense/event-schemas",
  "version": "0.1.0",
  "private": true,
  "description": "Kafka topic taxonomy, event envelope and per-eventType payload schemas/types shared between apps/api, apps/outbox-publisher and apps/vision-service (PRD-Phase-3).",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "tsc -p tsconfig.json && node --test dist/*.test.js"
  },
  "devDependencies": {
    "@ai-defense/eslint-config": "workspace:*",
    "@ai-defense/ts-config": "workspace:*",
    "@types/node": "^22.10.2",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2"
  }
}
