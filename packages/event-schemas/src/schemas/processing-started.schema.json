{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ai-defense-platform/schemas/processing-started.schema.json",
  "title": "ProcessingStartedPayload",
  "description": "Payload for PROCESSING_STARTED (eventVersion 1), published to aidefense.processing-events by apps/vision-service's stub consumer (PRD-Phase-3 REQ-3.13) on receipt of MISSION_PROCESSING_REQUESTED.",
  "type": "object",
  "additionalProperties": true,
  "required": ["missionId"],
  "properties": {
    "missionId": {
      "type": "string",
      "format": "uuid"
    }
  }
}
