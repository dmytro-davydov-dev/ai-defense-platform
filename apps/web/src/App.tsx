import "./App.css";

/**
 * Placeholder shell — Phase 1 (docs/mvp-plan/PRD-Phase-1.md, REQ-1.3).
 * The real Mission Workspace (auth, mission list/detail, upload, video
 * overlays, GIS) is built in Phase 6.
 */
function App() {
  return (
    <main className="app-shell">
      <h1>AI Defense Platform</h1>
      <p>Mission workspace — Phase 1 scaffold.</p>
      <p className="status">
        This shell will host the operator Mission Workspace starting in Phase 6 of the roadmap.
      </p>
    </main>
  );
}

export default App;
