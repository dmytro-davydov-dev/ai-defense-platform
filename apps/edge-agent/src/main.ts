import { log } from "@ai-defense/observability";

/**
 * Empty stub — Phase 1 (docs/mvp-plan/PRD-Phase-1.md, REQ-1.7).
 *
 * Implemented in Phase 9 (edge runtime / Jetson deployment) per
 * docs/mvp-plan/MVP_Implementation_Plan.md's roadmap scope — explicitly
 * deferred past the MVP (Phases 1-7).
 */
function main(): void {
  log("info", "edge-agent stub — no-op until Phase 9");
}

main();
